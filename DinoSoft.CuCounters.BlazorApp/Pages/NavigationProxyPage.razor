@*
    Звездочка обязательна, чтобы передавть слешы. 
    Подробнее тут https://docs.microsoft.com/ru-ru/aspnet/core/blazor/fundamentals/routing?view=aspnetcore-5.0
*@
@page "/navigation-proxy/{pageRoute}"
@inject NavigationManager navigationManager

<LayoutView>
</LayoutView>


@code {

    [Parameter]
    public string pageRoute { get; set; }

    protected override async Task OnInitializedAsync()
    {
        navigationManager.NavigateTo(pageRoute);
    }
}